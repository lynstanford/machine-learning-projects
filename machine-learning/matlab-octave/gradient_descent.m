% Based on a differentiable function (no vertical tangents)
y = (x + 5)^2;

% Step 1 - Let's start from a random point
x = -3;

% Find y:
x = sqrt(y - 5);

% Then find the gradient of the function, or 1st order derivative w.r.t. the function
diff(y) / diff(x) = 2 * (x + 5);

